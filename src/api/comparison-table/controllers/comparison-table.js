'use strict';

/**
 * comparison-table controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::comparison-table.comparison-table');
